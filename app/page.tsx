'use client';

import { useMemo, useState } from 'react';
import { AlertCircle, ArrowLeft, ArrowRight, ArrowUpRight, BookOpen, CheckCircle2, ClipboardCheck, Clock3, Download, ExternalLink, FileSearch, GraduationCap, Layers3, Library, Route, Search, ShieldCheck, Target, TimerReset } from 'lucide-react';
import { SidebarProvider, Sidebar, SidebarContent, SidebarFooter, SidebarHeader, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarTrigger } from '@/components/ui/sidebar';
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Progress } from '@/components/ui/progress';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
import { Checkbox } from '@/components/ui/checkbox';
import { capstoneTasks, getPlan, intensiveWeeks, lessonExtras, lessons, sourceBank, stages, trackNames, type Lesson, type Track } from '@/lib/course';
import sources from '@/lib/sources.json';

const nav=[{label:'总控台',icon:Target},{label:'章节精讲',icon:BookOpen},{label:'高压计划',icon:Route},{label:'模拟考场',icon:ClipboardCheck},{label:'资料库',icon:Library}];

function download(name:string,text:string){const url=URL.createObjectURL(new Blob(['\ufeff'+text],{type:'text/markdown;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),700);}
function Heading({tag,title,body,stamp}:{tag:string;title:string;body:string;stamp?:string}){return <div className="page-heading"><div><p className="eyebrow">{tag}</p><h1>{title}</h1><p>{body}</p></div>{stamp?<span className="course-label">{stamp}</span>:null}</div>}
function Sources({ids}:{ids:string[]}){return <div className="source-chain">{ids.map(id=>{const item=sourceBank.find(x=>x.id===id);return item?<a href={item.url} key={id} target="_blank" rel="noreferrer">{item.authority} · {item.title}<ExternalLink size={13}/></a>:null;})}</div>}

export default function Home(){
 const [view,setView]=useState('总控台');
 const [track,setTrack]=useState<Track>('common');
 const [lessonId,setLessonId]=useState<string|null>(null);
 const [answers,setAnswers]=useState<Record<string,number>>({});
 const [notes,setNotes]=useState<Record<string,string>>({});
 const [practice,setPractice]=useState<Record<string,boolean[]>>({});
 const [submitted,setSubmitted]=useState<Record<string,boolean>>({});
 const [passed,setPassed]=useState<string[]>([]);
 const [hours,setHours]=useState(12);
 const [query,setQuery]=useState('');
 const [libraryTab,setLibraryTab]=useState('official');
 const [examDraft,setExamDraft]=useState<Record<string,string>>({});
 const [examMessage,setExamMessage]=useState('');
 const lesson=lessons.find(x=>x.id===lessonId);
 const plan=getPlan(hours);
 const next=lessons.find(x=>!passed.includes(x.id))||lessons[0];
 const completed=(t:Track)=>lessons.filter(x=>x.track===t&&passed.includes(x.id)).length;
 const report=useMemo(()=>[
  '# 标途 · 高强度训练记录','',
  '总训练量：'+plan.total+'小时；每周：'+plan.hours+'小时；预计：'+plan.weeks+'周。',
  '章节通过：'+passed.length+'/'+lessons.length,'',
  ...lessons.flatMap(x=>['## '+x.id.toUpperCase()+' '+x.title,'状态：'+(passed.includes(x.id)?'达到在线自检门槛':'未通过或未完成'),'学习记录：'+(notes[x.id]||'未填写'),'']),
  '## 综合压测',...capstoneTasks.flatMap(x=>['### '+x.title,examDraft[x.id]||'未填写','']),
  '## 导师复核说明','在线通过不代表独立项目授权。导师须另行复核交付质量、真实性、异常处置和真实项目跟岗。'
 ].join('\n'),[plan,passed,notes,examDraft]);
 function go(name:string){setView(name);setLessonId(null);setExamMessage('');window.scrollTo({top:0,behavior:'smooth'});}
 function open(item:Lesson){setView('章节精讲');setLessonId(item.id);window.scrollTo({top:0,behavior:'smooth'});}
 function submitLesson(){if(!lesson)return;setSubmitted({...submitted,[lesson.id]:true});const checks=practice[lesson.id]||[false,false,false];const allCorrect=lesson.questions.every((q,i)=>answers[lesson.id+'-'+i]===q.correct);if(allCorrect&&checks.every(Boolean)&&(notes[lesson.id]||'').trim().length>=100)setPassed(x=>x.includes(lesson.id)?x:[...x,lesson.id]);}
 return <SidebarProvider><Sidebar className="app-sidebar"><SidebarHeader><div className="brand"><span className="brand-symbol"><Layers3 size={24}/></span><div>标途<span>招投标实战训练营</span></div></div></SidebarHeader><SidebarContent><div className="nav-label">20周 · 240小时</div><SidebarMenu>{nav.map(({label,icon:Icon})=><SidebarMenuItem key={label}><SidebarMenuButton isActive={view===label} onClick={()=>go(label)}><Icon/><span>{label}</span></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarContent><SidebarFooter><div className="sidebar-note"><GraduationCap/><strong>以交付物证明能力</strong><p>讲解 · 限时练习 · 周测 · 错题回炉 · 导师答辩</p></div><button className="text-button" onClick={()=>download('标途-训练记录.md',report)}><Download size={16}/> 导出训练记录</button><small>当前为试用版，刷新前请导出。<br/>在线自检不等于获得项目授权。</small></SidebarFooter></Sidebar><div className="workspace"><header className="topbar"><div><SidebarTrigger/><span>训练营 / {view}{lesson?' / '+lesson.id.toUpperCase():''}</span></div><span className="edition">建设工程 · 招标组织＋投标实务 <i/> 高强度训练</span></header><main className="main">
 {view==='总控台'&&!lesson&&<><Heading tag="INTENSIVE CAMP · DAY 01" title="把小白训练成能扛项目的人。" body="每一章都先讲术语、场景、判断顺序和常见误区，再做限时练习、闭卷检查和交付物。不是看完就算学会。" stamp="20周 · 240小时"/>
 <div className="intensive-banner"><div><span className="intensity-mark">TODAY'S MISSION</span><h2>{next.id.toUpperCase()} · {next.title}</h2><p>{next.summary}</p><div className="mission-list"><span><Clock3 size={15}/>建议 {next.hours} 小时</span><span><ShieldCheck size={15}/> {next.gate}</span></div><button className="primary" onClick={()=>open(next)}>进入老师讲解 <ArrowRight size={17}/></button></div><div className="scoreboard"><span>在线自检通过</span><strong>{passed.length}<small> / 18章</small></strong><Progress value={passed.length/18*100} aria-label="课程进度"/><p>通过条件：三步练习、100字学习记录、三道闭卷题全对。</p></div></div>
 <div className="dashboard-grid"><section><div className="section-heading"><h2>全流程，两条线同步理解</h2><span>每个阶段都要知道自己该做什么、不该做什么</span></div><div className="process-grid">{stages.map((x,i)=><button className="process-item" key={x.title} onClick={()=>go('章节精讲')}><span>{String(i+1).padStart(2,'0')}</span><strong>{x.title}</strong><ArrowUpRight size={15}/></button>)}</div><div className="section-heading"><h2>三道关卡</h2><span>基础不过，不建议跳到专业线</span></div><div className="pressure-grid"><article><b>第一关</b><h3>公共基础 6章</h3><p>理解项目、制度、门槛、文件、节点和电子证据，先避免做错程序。</p><button onClick={()=>{setTrack('common');go('章节精讲')}}>进入基础课 <ArrowRight size={15}/></button></article><article><b>第二关</b><h3>招标组织 6章</h3><p>从需求、资格、文件、答疑到开评标和归档，练组织能力与边界意识。</p><button onClick={()=>{setTrack('org');go('章节精讲')}}>进入组织线 <ArrowRight size={15}/></button></article><article><b>第三关</b><h3>投标实务 6章</h3><p>从决策、证据、响应、技术商务、递交到履约，练可交付的真实能力。</p><button onClick={()=>{setTrack('bid');go('章节精讲')}}>进入投标线 <ArrowRight size={15}/></button></article></div></section><aside className="right-column"><div className="panel"><p className="eyebrow">SIX DAYS A WEEK</p><h3>每周最低训练量</h3><ul className="strict-list"><li>概念与法规记忆 <b>60分钟</b></li><li>原始文件定位 <b>90分钟</b></li><li>限时交付练习 <b>120分钟</b></li><li>闭卷周测 <b>80分</b></li></ul><button className="text-button" onClick={()=>go('高压计划')}>查看20周节奏 <ArrowRight size={15}/></button></div><div className="panel tinted"><p className="eyebrow">CURRENT PROGRESS</p><h3>三线通过情况</h3>{(Object.keys(trackNames) as Track[]).map(t=><div className="track-progress" key={t}><span>{trackNames[t]}</span><b>{completed(t)} / 6</b></div>)}<p>任何错题和缺交付都保留在补训清单中，不用“看过了”掩盖。</p></div></aside></div></>}

 {view==='章节精讲'&&!lesson&&<><Heading tag="TEACHER-LED CHAPTERS" title="每一章都有老师式拆解。" body="进入章节后会看到：真实工作场景、术语解释、判断顺序、老师示范、初学者误区、分步骤练习、闭卷检查与能力验收。" stamp={trackNames[track]+' · '+completed(track)+'/6通过'}/><Tabs value={track} onValueChange={v=>setTrack(v as Track)}><TabsList className="path-tabs training-tabs">{(Object.keys(trackNames) as Track[]).map(t=><TabsTrigger key={t} value={t}>{trackNames[t]}（{completed(t)}/6）</TabsTrigger>)}</TabsList></Tabs><div className="course-intro"><BookOpen size={22}/><p>{track==='common'?'零基础必须从这里开始：先把术语、制度、文件和节点放进同一张地图。':'专业线不假设你已经会做；每个章节都会先复习基础，再告诉你在这个岗位如何判断和交付。'}</p></div><div className="chapter-grid">{lessons.filter(x=>x.track===track).map((item,i)=><article className={'chapter-card '+(passed.includes(item.id)?'chapter-done':'')} key={item.id}><div className="chapter-card-top"><span>第 {item.week} 周 · {item.id.toUpperCase()}</span>{passed.includes(item.id)?<CheckCircle2 size={18}/>:<Clock3 size={18}/>}</div><h2>{item.title}</h2><p>{item.summary}</p><div className="chapter-skills">{lessonExtras[item.id].ability.map(x=><span key={x}>{x}</span>)}</div><div className="chapter-card-foot"><small>老师讲解 {item.knowledge.length} 节 · {item.hours} 小时</small><button className="secondary" onClick={()=>open(item)}>开始第 {i+1} 章 <ArrowRight size={15}/></button></div></article>)}</div></>}

 {view==='章节精讲'&&lesson&&<><button className="back-button" onClick={()=>setLessonId(null)}><ArrowLeft size={16}/> 返回章节目录</button><Heading tag={trackNames[lesson.track]+' · 第'+lesson.week+'周 · '+lesson.id.toUpperCase()} title={lesson.title} body={lesson.summary} stamp={'建议 '+lesson.hours+' 小时 · 限时交付'}/><div className="lesson-layout"><section className="lesson-content"><div className="teacher-opening"><GraduationCap size={26}/><div><b>本章结束时，你必须掌握的能力</b><p>{lesson.goal}</p></div></div><div className="teacher-scene"><p className="eyebrow">老师先带你进入场景</p><h2>你会遇到什么</h2><p>{lessonExtras[lesson.id].scene}</p><div><b>老师示范怎么想</b><p>{lessonExtras[lesson.id].example}</p></div></div><div className="knowledge-section"><p className="eyebrow">第一部分 · 核心讲解</p><h2>先理解判断顺序，再开始做事</h2>{lesson.knowledge.map((point,i)=><article className="knowledge-card" key={point.title}><span>{String(i+1).padStart(2,'0')}</span><div><h3>{point.title}</h3><p>{point.detail}</p></div></article>)}</div><div className="memory-box"><p className="eyebrow">第二部分 · 记忆框</p><h2>本章必须记牢</h2><ul>{lesson.memory.map(x=><li key={x}>{x}</li>)}</ul></div><div className="mistake-box"><p className="eyebrow">第三部分 · 初学者误区</p><h2>老师提前替你踩坑</h2>{lessonExtras[lesson.id].mistakes.map((x,i)=><p key={x}><b>{i+1}.</b>{x}</p>)}</div><div className="practice-lab"><p className="eyebrow">第四部分 · 分步骤练习</p><h2>照着做，不要跳步</h2><p className="practice-context">{lesson.task}</p>{lesson.drills.map((step,i)=>{const list=practice[lesson.id]||[false,false,false];return <label className="practice-step" key={step}><Checkbox checked={list[i]} onCheckedChange={v=>{const copy=[...list];copy[i]=v===true;setPractice({...practice,[lesson.id]:copy});}}/><span><b>步骤 {i+1}</b>{step}</span></label>})}<div className="output-label"><b>本章交付物</b>{lesson.deliverable}</div><label htmlFor="note"><b>写给老师的学习记录（至少100字）</b><span>请写明：我依据了什么、还不确定什么、准备如何复核或上报。</span></label><textarea id="note" value={notes[lesson.id]||''} onChange={e=>setNotes({...notes,[lesson.id]:e.target.value})} rows={8} placeholder="不要只写“我学会了”。请用本章场景、规则和判断顺序写出你的思考。"/></div><div className="quiz"><p className="eyebrow">第五部分 · 闭卷检查</p><h2>三题全对才算理解</h2>{lesson.questions.map((item,i)=>{const key=lesson.id+'-'+i;return <div className="quiz-question" key={item.prompt}><b>第 {i+1} 题</b><p>{item.prompt}</p><RadioGroup value={answers[key]===undefined?'':String(answers[key])} onValueChange={v=>setAnswers({...answers,[key]:Number(v)})} aria-label={item.prompt}>{item.options.map((o,j)=><label className="answer-option" htmlFor={key+'-'+j} key={o}><RadioGroupItem id={key+'-'+j} value={String(j)}/><span>{o}</span></label>)}</RadioGroup>{submitted[lesson.id]?<div className={'mini-feedback '+(answers[key]===item.correct?'correct':'incorrect')}><strong>{answers[key]===item.correct?'正确':'回到讲解复习'}</strong><span>{item.explanation}</span></div>:null}</div>})}<button className="primary" disabled={!lesson.questions.every((_,i)=>answers[lesson.id+'-'+i]!==undefined)} onClick={submitLesson}>提交本章闭卷检查 <ArrowRight size={16}/></button>{submitted[lesson.id]?<div className={'feedback '+(passed.includes(lesson.id)?'correct':'incorrect')}><strong>{passed.includes(lesson.id)?'已达到在线自检门槛':'还不能通过本章'}</strong><p>{passed.includes(lesson.id)?'请导出记录交导师确认。真实项目仍需按组织授权办理。':'你还需完成三步练习、100字记录，并让三道闭卷题全部正确。'}</p></div>:null}</div></section><aside className="lesson-aside"><div className="panel ability-panel"><p className="eyebrow">ABILITY TARGET</p><h3>学完要会什么</h3><ul>{lessonExtras[lesson.id].ability.map(x=><li key={x}>{x}</li>)}</ul></div><div className="panel"><p className="eyebrow">SOURCE CHAIN</p><h3>本章依据</h3><Sources ids={lesson.sourceIds}/><button className="text-button" onClick={()=>go('资料库')}>打开资料库 <ExternalLink size={15}/></button></div><div className="panel warning-panel"><TimerReset size={20}/><h3>本章硬门槛</h3><p>{lesson.gate}</p></div></aside></div></>}

 {view==='高压计划'&&!lesson&&<><Heading tag="GAOKAO-STYLE RHYTHM" title="20周，不留“学过但不会”的空档。" body="默认每周12小时、每周6天训练。低于8小时会明显拉长周期；每周必须有可检查成果，80分以下补训，红线题必须满分。" stamp={plan.total+'小时 · 预计'+plan.weeks+'周'}/><div className="plan-banner intense-plan"><div><span className="pill light">压力来自清晰门槛，不来自盲目熬夜</span><h2>每周训练负荷：{plan.hours} 小时</h2><p>建议周一至周五各2小时，周末2小时限时卷与错题回炉。训练计划不能替代睡眠、健康或真实项目中的授权流程。</p></div><label className="hours-control">每周可投入小时数<input type="number" min="8" max="24" value={hours} onChange={e=>setHours(Number(e.target.value))} onBlur={()=>setHours(plan.hours)}/><span>8—24小时</span></label></div><div className="phase-roadmap">{plan.phases.map((x,i)=><article key={x.name}><span>0{i+1}</span><div><p className="eyebrow">约 {x.hours} 小时</p><h2>{x.name}</h2><p>{x.goal}</p></div><button className="secondary" onClick={()=>go(i===3?'模拟考场':'章节精讲')}>进入阶段 <ArrowRight size={15}/></button></article>)}</div><div className="section-heading"><h2>20周训练周历</h2><span>默认12小时／周；每周都设置补训触发器</span></div><div className="week-table">{intensiveWeeks.map(x=><article key={x[0]}><strong>{x[0]}</strong><div><b>{x[1]}</b><p>本周交付：{x[2]}</p></div><span>{x[3]}</span></article>)}</div><div className="ability-grid"><div className="panel"><span className="ability-level">L1</span><h3>看懂并能定位</h3><p>能解释角色、路径、门槛、文件和节点，能回到原文定位依据。</p></div><div className="panel"><span className="ability-level">L2</span><h3>在指导下完成</h3><p>能按检查表交付单项工作，发现不确定事项并保留证据、及时升级。</p></div><div className="panel"><span className="ability-level">L3</span><h3>授权范围内推进</h3><p>需通过双线压测、导师答辩和真实项目跟岗。时长与在线答题不能自动获得此等级。</p></div></div><div className="action-row"><button className="primary" onClick={()=>download('标途-20周计划.md',intensiveWeeks.map(x=>'## '+x[0]+'\n重点：'+x[1]+'\n交付：'+x[2]+'\n门槛：'+x[3]).join('\n\n'))}><Download size={16}/> 导出训练计划</button><a href="/implementation-plan.md" download>下载系统建设规划 <ArrowUpRight size={15}/></a></div></>}

 {view==='模拟考场'&&!lesson&&<><Heading tag="SIMULATED EXAMINATION" title="不是漂亮作业，是限时扛住项目。" body="全部情景为教学模拟。目标是检验事实判断、资料定位、交付质量和异常处置，不替代真实项目法定程序。" stamp="双线综合压测 · 5关"/><div className="exam-rules"><ShieldCheck size={23}/><div><h2>考场规则</h2><p>先列事实和缺口，再写结论；每项规则写来源；不得编造材料、审批或他人行为；导师可追问“依据在哪、谁决定、怎么留痕”。</p></div></div><div className="studio-grid">{capstoneTasks.map(x=><article className="studio-card exam-card" key={x.id}><span className="type-label">限时考核</span><h2>{x.title}</h2><p>{x.hint}</p><div className="rubric"><b>老师按什么评分</b><p>{x.rubric}</p></div><label htmlFor={'exam-'+x.id}>我的答卷草稿</label><textarea id={'exam-'+x.id} rows={9} value={examDraft[x.id]||''} onChange={e=>{setExamDraft({...examDraft,[x.id]:e.target.value});setExamMessage('');}} placeholder="写事实、依据、判断、责任人、时间和证据……"/></article>)}</div><div className="review-panel"><div><h2>评分与淘汰规则</h2><p>建议标准：事实与来源25分，文件交付35分，节点与异常25分，留痕复盘15分。综合80分才进导师复核；真实性、程序、签章、递交红线不以总分抵消，直接回到对应章节补训。</p></div><div className="action-row"><button className="secondary" onClick={()=>{const missing=capstoneTasks.filter(x=>!(examDraft[x.id]||'').trim());setExamMessage(missing.length?'尚有'+missing.length+'关未填写：'+missing.map(x=>x.title).join('、'):'五关均已填写。仅检查内容存在；质量与真实性由导师复核。');}}>检查完整性</button><button className="primary" onClick={()=>download('标途-综合压测答卷.md',report)}><Download size={16}/> 导出交导师</button></div>{examMessage?<p className="feedback">{examMessage}</p>:null}</div></>}

  {view==='资料库'&&!lesson&&<><Heading tag="RESEARCHED KNOWLEDGE BASE" title="先看权威依据，再看范本和案例。" body="课程骨架来自官方发布机关。地方范本、历史项目、学习笔记用于解释和练习，不能取代当期有效规则。" stamp={sourceBank.length+'项权威来源 · '+sources.materials.length+'份本地材料'}/><div className="research-note"><FileSearch size={22}/><p>新增研究并纳入司法部国家行政法规库、国家发展改革委第16号令、电子招投标办法、公告公示办法、公平竞争审查规则、招标人主体责任指引，以及财政部政府采购实施条例、质疑投诉办法。每项都能打开原文。</p></div><div className="library-toolbar"><Tabs value={libraryTab} onValueChange={v=>setLibraryTab(String(v))}><TabsList className="path-tabs training-tabs"><TabsTrigger value="official">官方依据</TabsTrigger><TabsTrigger value="local">本地范本与案例</TabsTrigger><TabsTrigger value="portal">宿州抓取目录</TabsTrigger></TabsList></Tabs><label className="search-box"><Search size={17}/><input aria-label="搜索资料" value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索标题、机关或主题…"/></label></div>{libraryTab==='official'?<div className="materials-grid">{sourceBank.filter(x=>(x.title+x.authority+x.note).includes(query.trim())).map(x=><article className="material-card official-card" key={x.id}><span className="type-label">{x.authority}</span><h3>{x.title}</h3><p>{x.note}</p><a href={x.url} target="_blank" rel="noreferrer">打开官方来源 <ExternalLink size={15}/></a></article>)}</div>:null}{libraryTab==='local'?<div className="materials-grid">{sources.materials.filter(x=>(x.title+x.kind+x.note).includes(query.trim())).map(x=><article className="material-card" key={x.id}><span className="type-label">{x.kind}</span><h3>{x.title}</h3><p>{x.note}</p><div className="material-footer"><small>{x.status}</small><a href={x.url} target="_blank" rel="noreferrer">打开原件 <ArrowUpRight size={15}/></a></div></article>)}</div>:null}{libraryTab==='portal'?<><div className="notice"><AlertCircle size={19}/><p>宿州国家政策法规栏目已抓取40条索引，其中35页取得较长文本、5页仍需从附件或外链补取正文。抓取成功不等于现行有效；正式题库以官方依据和专业审核为准。</p></div><div className="policy-list">{sources.policies.filter(x=>x.title.includes(query.trim())).map(x=><article className="policy-row" key={x.url}><div><h3>{x.title}</h3><p>{x.note}</p><small>{x.status}</small></div><a href={x.url} target="_blank" rel="noreferrer">宿州来源 <ExternalLink size={15}/></a></article>)}</div></>:null}</>}
 </main></div></SidebarProvider>;
}
